version: '3'

services:
  mcpc:
    build: ./pe
    container_name: mcpc
    ports:
      - "25565:25565"
    volumes:
      - ./pc:/app:rw

  mcpe:
    build: ./pe
    container_name: mcpe
    ports:
      - "19132:19132"
    volumes:
      - ./pe:/app:rw
